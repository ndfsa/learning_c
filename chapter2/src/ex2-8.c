#include "ch2utils.h"
#include <stdio.h>

main() {
    /* Write a function rigthrot(x, n) that returns the value of the integer x rotated to the right
     * by n bit positions.
     * */

    printf("%d\n", rightrot(12, 2));
}
